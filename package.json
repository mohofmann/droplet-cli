{
  "name": "droplet-cli",
  "version": "1.0.0",
  "description": "A CLI for rapidly managing Digital Ocean Droplets",
  "main": "index.js",
  "scripts": {
    "build": "browserify --node index.js > bundle.js && terser -c -m --verbose bundle.js > hummelcli.js && rm bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Moritz Hofmann",
  "license": "MIT",
  "dependencies": {
    "prompts": "^2.3.2"
  },
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  }
}
