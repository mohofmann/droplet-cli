{
  "name": "hummel-cli",
  "version": "1.0.0",
  "description": "Command Line Interface for the Hummelpuff Minecraft Server",
  "main": "index.js",
  "scripts": {
    "build": "browserify --node index.js > bundle.js && terser -c -m --verbose bundle.js > hummelcli.js && rm bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Moritz Hofmann",
  "license": "ISC",
  "dependencies": {
    "prompts": "^2.0.2"
  },
  "devDependencies": {
    "webpack-cli": "^3.2.3"
  }
}
